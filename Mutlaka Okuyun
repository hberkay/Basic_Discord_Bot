First, you need to download the node.js 
After that, write the folder (which is including your project) "npm install discord.js" with the terminal. That will download a library named Axios.
Go inside index.js and type your discord bot token to the client.login("discord bot token"). After that type node .\index.js to the terminal. That will  run your bot
